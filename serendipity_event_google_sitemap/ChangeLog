0.69:
-----
 * Fix an uninitialized PHP 8 parameter - removes a ping PHP < 5.6 checkup

0.68:
-----
 * Fix an uninitialized PHP 8 $eventData parameter

0.67:
-----
 * Fix a doublet

0.66:
-----
 * Fix PHP 8 Required parameter $eventData follows optional parameter $basefilename
 * Fix case 'types_to_add': imploding $types

0.65:
-----
 * Fix a non-isset parameter $max variable for PHP 8
 * Check $cur['timestamp'] for PHP 8
 * Set PHP requirement to PHP 7

0.64:
-----
 * By default do not add pages that modern themes will set as noindex (overview pages)

0.63:
-----
 * Fix unknown message constant and fix to sub check for lowercased 'unknown'
   entryproperties property value match (custom permalinks plugin). They both
   tried to access a UNKNOWN constant, which did not exist.

0.62:
-----
 * Set xmlns for sitemap-news also with gnews_single mode

0.61:
-----
 * Fix 'sqlite3oo' case check again

0.60:
-----
 * Fix 'pdo-sqliteoo' case check
 * Replace default submissions.ask.com example PLUGIN_EVENT_SITEMAP_URL
   with https://www.bing.com/webmaster/ping.aspx?siteMap=%s
 * Fix some typos
 
0.59:
-----

 * Check REQUEST object
 * Set plugin consistencies
 * Use load language API - extends required Serendipity version to v.1.6


0.58:
-----

Exclude archives/Px.html pages when the "archives" option is not checked

0.57:
-----

Forward compatibility to s9y's new defaultBaseURL variable, patch by Manko10

0.56:
-----

Several changes to optimize the news_sitemap and sitemap.xml differences, and a new option
to toggle whether sitemap.xml contains any NEWS references

0.53:
-----

Create a second, smaller 'news_sitemap.xml' file that's suitable for submitting to google news separately.

0.52:
----

Proper $min reading, fixed too large sitemap files, thanks to Freudi

0.51:
-----

Also add config options for subscription and genre tags

0.50:
-----

Make GoogleNews BlogTitle configurable

0.49:
----

Removed google news 'subscription' format, as it was misunderstood.

0.48:
-----

Support new google news XML

0.47:
----

Do not add links to not yet published entries, thanks to Stefan

0.46:
----

Do not add UNKNOWN permalinks

0.44:
-----

Made google news output optional.

0.42:
-----

* Add google NEWS XML output, including meta keywords
