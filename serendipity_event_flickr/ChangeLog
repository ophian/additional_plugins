1.0.0:
------
 * Declare strict types
 * Re-/Move UTF-8 lang file to plugin root for Styx 5.0
 * Use native htmlspecialchars()
 * Raise requirements for Styx 5.0

0.8:
----
 * Secure external target _blank links by living standard rel="noopener"

0.7:
----
 * Sync [de] translation

0.6:
----
 * Use load language API - extends required Serendipity version to v.1.6
 * Check missing REQUEST url (Request2) switch for phpFlickr/phpFlickr.php
   fnc phpFlickr() which is unsolved, since being a weird request without url.
   This might break when removing the old HTTP Request class in core.

0.5.1:
------
 * Properly use the example() API method by returning, not echo'ing. Unimportant update.
